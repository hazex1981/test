
<div data-role="header" data-position="fixed" data-theme="b"
 style="
	width:800px;
    margin-left : auto ; margin-right : auto;"



>
  <div data-role="navbar">
    <ul>
      <li><%= link_to "検索",categories_path %></li>
      <li><%= link_to "メッセージ",messages_path %></li>
      <li><%= link_to "プロフィール",profile_user_path(@current_user.id) %></li>
    </ul>
  </div>
</div>


<div data-role="content"
  style="background:none repeat-x scroll 0 0 #e6e6fa;
	width:770px;
	height:800px;
	margin-left : auto ; margin-right : auto;"


>

  <h3>エキスパート詳細</h3>
    <ul data-role="listview">
  <li class="expert_list"  style="background: #e6e6fa">
  
        <%= image_tag(@expert.image,
            :size => '65x65',
            :class =>'user_image') %>

        <%= link_to @expert.name + "さん "+" 2000円／5メッセージ",expert_path(@expert.id) %>
         <% @categorynames = @expert.categories.map{|m| m.name} %>

        <% @i=1 %>
        <% @categorynames.each do |name| %>

        <% if( @i<=5) %>
        <span class="expert_tag" >
        
        <%= @i %>
        <%= name %>
        </span>
        <% end %>
        <% @i+=1 %>
   <% end %>

  
      </li>

   </ul>



  <p>(専門) :
  
  <% @categorynames = @expert.categories.map{|m| m.name} %>
  <% @categorynames.each do |name| %>

  <span class="expert_tag"
  >
  <%= name %>
  </span>

   <% end %>
  </p>
   <p>(メッセージ単価)：　¥２０００円／５メッセージ</p>
   <p>(経歴詳細)： 詳細データ</p>
   <p>(時間・注意)：月ー金：AM:8:30-9:30 PM:21:00-23:00頃</p>
  
  <!-- セッションを始めるためのリンク -->
  <p><%= link_to "セッションスタート",feed_message_path(@expert.id,:role => "user") %></p>

</div>





